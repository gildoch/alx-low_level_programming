#include <stdio.h>
/**
  * main - Entry point
  * @argc: Argument count
  * @argv: Array of arguments
  * Return: Int
  */

int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
